apiVersion: v1
kind: Service
metadata:
  name: ${{values.component_id}}
  labels:
    app: ${{values.component_id}}
spec:
  selector:
    app: ${{values.component_id}}
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
